---
title: "订阅号开发者模式自研记录"
date: 2017-06-03 15:11:40 +0000
categories: ["python"]
tags: ["Pages"]
description: "订阅号 开发者模式自研记录 2017 年 6 月 3 日 23:11 需求：开启订阅号的开发者模式，并将用户消息发送往自己的服务器中。 流程： 首先，激活开发者模式 激活后会生成一个 appid 和 AppSecret， 需要自己记下来，平台不会给你记录。 配置 Ip 白名单。即自己服务器的 IP ，否则发送到微信接口"
source: "evernote-local-db"
---

订阅号
开发者模式自研记录
2017
年
6
月
3
日
23:11
需求：开启订阅号的开发者模式，并将用户消息发送往自己的服务器中。
流程：
首先，激活开发者模式

激活后会生成一个
appid
和
AppSecret，
需要自己记下来，平台不会给你记录。
配置
Ip
白名单。即自己服务器的
IP
，否则发送到微信接口的数据被拒绝。
服务器配置。这里先点击修改配置。需要填写服务器接收消息的
api
的
url
，
token
和加密令牌。
其中，
token
是用户自定义的，但是要求服务器
api
中与微信配置页面中的保持一致，这使用来让微信发送验证请求然后后台回报回去验证的。加密令牌也是自定义或者微信随机的，用来做加密数据传输用的，非必须用。
因此这一步已经要求你的服务器配置
ok
了。
自己服务器的配置。要求有
web
接口，而且是
80
或者
443
。我这里用的
flask
框架，写了个
weixin/interface
接口，内容如下：

@app.route('/wechat/interface/',methods=['POST','GET'])
def interface():

'''
微信公众号
yaung_jean
的开发者接口

TOKEN
为用户自定义的，但是需要与开发者页面上的一致。
'''

params=request.args if request.method == 'GET' else request.form

if request.method == 'GET':

signature=params.get('signature')

timestamp=params.get('timestamp')

nonce=params.get('nonce')

TOKEN='83j2jdiwh2HSUD'

echostr=params.get('echostr')

tmparr=(TOKEN,timestamp,nonce)

tmpStr=''.join(sorted(tmparr))

import hashlib

ser_tmpStr=hashlib.sha1(tmpStr).hexdigest()

if ser_tmpStr == signature:

return echostr

return False

else:

#
这里只能用
request.data,
用不了
params
，不知道原因

print request.data

print params

return "ok"
这里接口就
ok
了。将代码中自定义的
TOKEN
复制到配置页面中，配置如下：
，点击提交，
ok
，连通完毕。
获取微信后台
iplist
，这是其中一个小功能而已。后端代码如下：
def get_acc_token():

'''
根据公众号的
appid
和私钥获取
access_token'''

re_url="https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential
&
appid=wx74cba41c1177d20f
&
secret=5cde7b97bac5e849cdaf92aecbefe129"

_result=urllib2.urlopen(re_url).read()

acc_token=json.loads(_result).get('access_token')

expires_in=json.loads(_result).get('expires_in')

return acc_token,expires_in

@app.route('/wechat/getinfo/',)
def get_wechat_info():

'''
获取微信服务器
IP
列表
'''

acc_token,period_secends=get_acc_token()

wechat_ip_url="https://api.weixin.qq.com/cgi-bin/getcallbackip?access_token=%s" % acc_token

_result=urllib2.urlopen(wechat_ip_url).read()

wechat_ip_list=json.loads(_result).get('ip_list')

print wechat_ip_list

if len(wechat_ip_list)>0:

return json.dumps(wechat_ip_list)

return "IP list found"

效果如下：
真正用得到的功能还没开始测试，待续。

已使用 Microsoft OneNote 2016 创建。
