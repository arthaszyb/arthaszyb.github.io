---
title: kill -0 PID
date: '2014-05-22'
description: 发送无效信号检查进程存在：若进程存在则 $? 为 0，否则为 1。
category: linux
tags:
  - shell-scripting
draft: false
source: evernote-local-db
lang: zh
---

向某一进程发送一个无效的信号，可用于检查进程是否存在：

```bash
kill -0 <PID>
echo $?
```

如果该进程存在（能够接收信号），`echo $?` 输出为 0，否则为 1。
